const lineEnd = {
  x: 0,
  y: 0
};
const lineStart = {
  x: 200,
  y: 41
};

const circleRadius = 20;
const ellipseWidth = 30;
const ellipseHeight = 20;

/*
 * DO
 *     NOT
 *          TOUCH
 * BELOW
 *     THIS
 *          COMMENT
 */

const inp = {
  lineStart: lineStart,
  lineEnd: lineEnd,
  circleRadius: circleRadius,
  ellipseWidth: ellipseWidth,
  ellipseHeight: ellipseHeight
};
